server.port=8888
spring.cloud.config.server.git.uri=https://github.com/TuPengXiong/spring-cloud-config-repo

#/{application}/{profile}[/{label}]
#/{application}-{profile}.yml
#/{label}/{application}-{profile}.yml
#/{application}-{profile}.properties
#/{label}/{application}-{profile}.properties

# application 指的是 spring.cloud.config.name
# profile 指的是 spring.active.profiles
# label 指的是 git的分支默认是master

# curl localhost:8888/cloud-config/development/1.0.0